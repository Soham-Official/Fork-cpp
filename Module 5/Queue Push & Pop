https://practice.geeksforgeeks.org/problems/queue-designer/1/?track=module-5-stacks-and-queues&batchId=113


queue<int>_push(int arr[],int n)
{
    queue<int>s;
   for(int i=0;i<n;i++)
   s.push(arr[i]);
   return s;
}

void _pop(queue<int>s)
{
     while(!s.empty())
    {
        cout<<s.front()<<" ";
        s.pop();
    }
} 
